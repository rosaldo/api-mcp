# Changelog

> Generated from `git` by `scripts/gen-changelog.sh` — do not edit by hand.

- **v1.10.1** (2026-09-04) · `fix` — a server declared on the operation is honoured, not just the document's: OpenAPI 3 allows servers at three levels and the closest one to the operation wins, and this read only the top — which is fine until an API splits itself across hosts, as EvoLink does by serving generation on api.evolink.ai and file uploads on files-api.evolink.ai from one document; every file call went to the generation host and came back 403, with the right address sitting in the description of the very tool that failed, so whoever investigated would read the answer and still not see it; --base-url keeps beating all three levels, because passing it is naming the destination on purpose and an override buried in a third party's document must not divert part of the traffic out of a test environment or proxy; both halves are pinned by tests that were watched failing, and the fix was proven end to end against the real API, where the call now reaches files-api and fails only on the fake key
- **v1.10.0** (2026-09-04) · `feat` — a header can carry {uuid}, a fresh id on every request: a whole family of APIs requires a unique id per call — eToro demands x-request-id on all 167 of its operations — and until now the only way to send one was --header with a fixed value, which repeats the same id forever and defeats the very purpose of the header; the expansion cannot live in Signature.Apply, where {timestamp} lives, because that one exists to share the instant that was signed while this one belongs to no signature and must work for APIs that authenticate with nothing at all, so it is a small applier of its own, chained ahead of whatever authentication was configured, and the two never see each other's placeholders; the tests pin both halves of why it exists, that three calls get three different v4s and that a header without the placeholder is left untouched, plus that the chain does not stop at its first member; this commit also puts the comments of v1.9.3 back into English, which they should have been
- **v1.9.3** (2026-09-03) · `fix` — a header carrying a placeholder is expanded, with the same instant the signature used: --header 'OK-ACCESS-TIMESTAMP={timestamp}' reached the server as the literal string, because the dialects apply fixed headers with req.Header.Set and never touched the value, so an API that wants the signed timestamp in a header of its own got the text {timestamp} and rejected the call; OKX answered 50112 Invalid OK-ACCESS-TIMESTAMP, which reads like clock drift and sends whoever investigates after the wrong thing — the clock was within a second of theirs and the header had simply never been filled, and the configuration that produced it even carried a comment promising that the tool guaranteed this, a promise nobody had checked; the expansion goes in Signature.Apply and not in each dialect for two reasons, one being that it is a single place instead of three, openapi graphql and discovery, and the other being that this is the ONLY point holding the same instant that was signed — expanding anywhere else would take its own now, and an API that signs the timestamp and also demands it in a header rejects the pair when they disagree, which would trade a bug that never works for one that works most of the time; the test asserts both halves, that no placeholder survives as text and that re-signing with the header's own stamp reproduces the signature that was sent, plus that a header without placeholders passes through untouched; the README gains the note next to the signature examples, since the flag's ability to carry them is what makes this class of API reachable at all
- **v1.9.2** (2026-09-02) · `fix` — a JSON spec is read with the JSON parser, a large one is given time to arrive, and both facts reach the documentation: one yaml.Unmarshal used to cover both formats, since YAML is a superset of JSON, and that holds everywhere except the single point where the two genuinely disagree — a key repeated inside the same object, which JSON allows and keeps the last of, while YAML refuses the document whole; published specs do exactly that, and one repeated line was enough to lose every operation in the file, reported as unrecognised spec, which points at the format instead of at the line responsible and leaves nothing to search for; the dialects already had this right, openapi.toJSON hands anything starting with a brace straight to the JSON parser, so the fix is to do the same in the two places that still read the raw document, the dialect detection and the title and description, the latter failing SILENTLY before this — server up, every tool working, no name and no instructions; the download bound goes from 30 seconds to two minutes, because multi-megabyte specs are ordinary now and one such URL measured 3s, 15s, 34s and 43s on four consecutive tries, so a 30-second bound makes a server that starts on some days and not others, the hardest failure to place since the spec is fine and the credentials are fine and nothing says the download ran out of time; and both changes reach the README and the architecture notes, which is where the previous shape of this work went wrong — neither change added a flag, so the flag table test could not catch the drift, and prose that quietly describes a version that no longer exists is only ever found by a reader; so the numbers stop depending on memory too: a test reads the CONSTANT on one side and the prose on the other, and refuses a value whose wording it does not know, which makes a bound that moves pass through the documentation to compile a green suite; six canaries watched failing first
- **v1.9.1** (2026-08-29) · `fix` — the server now announces the file prefix in its instructions, because a capability the model is not told about does not exist: --blob-in was configured, the process was running with it, and the model still sent a bare path and got it rejected by the API — nothing in the schema, the tool description or anywhere else said the prefix was there to be used, and no schema can say it since it applies to every string argument; the notice names the directory too, and stays EMPTY when the flag is off, because announcing a prefix that would be passed through untouched is worse than silence
- **v1.9.0** (2026-08-29) · `feat` — a path can now stand where the bytes go, which is the only way a model attaches a file it cannot carry: the asymmetry that made --blob-dir necessary runs the other way too, because attaching a 185 KB PDF to a Gmail message means producing the entire RFC 2822 message base64d AS AN ARGUMENT — a quarter of a megabyte the model has to hold and type out, and measured in the field the same send went through with a small attachment and was impossible with the real one; --blob-in names a directory, an argument starting with file: is read from it and base64d on the way to the API, and the model never sees the payload; reading is confined to that directory RESOLVED, since .. and a symlink pointing outside are exactly how a contained path stops being one, and the alphabet follows the dialect because Google declares its format byte fields as base64URL while OpenAPIs is plain base64
- **v1.8.2** (2026-08-26) · `chore` — the flag table is now checked against the real flag set, in both directions: a flag added without documenting it fails the build, and so does a documented flag that no longer exists — the section was called All flags while listing barely half of them
- **v1.8.1** (2026-08-26) · `docs` — the flag table and the architecture map catch up with what the tool actually does: --type still offered two dialects out of three, --depth was documented under its old GraphQL-only name, and --blob-dir was not there at all
- **v1.8.0** (2026-08-26) · `feat` — Google's discovery document becomes a dialect: they publish one for over three hundred of their APIs at a predictable address and no OpenAPI parser reads it, so writing this reader once makes Drive, Sheets, Calendar, Gmail and the rest reachable without a line of code per API
- **v1.7.0** (2026-08-26) · `feat` — responses can hand the model a file path instead of a megabyte of base64: an inlined image arrives as 1.17 MB in a single field, which is a third of a million tokens the model can neither look at nor save, so --blob-dir writes those bytes to disk and leaves the shape of the answer intact
- **v1.6.0** (2026-08-26) · `feat` — the server now tells the client what it is for, taken from the spec's own info: under tool search a client loads only tool names and the server instructions up front, so a server that says nothing about itself is one the model never searches
- **v1.5.0** (2026-08-23) · `refactor` — releases move to CI: pushing a tag now builds and publishes, and push.sh only ships commits and tag unless --full is given — Actions is free on public repositories, so the published binaries stop depending on whichever toolchain this workstation happens to have
- **v1.4.0** (2026-08-22) · `feat` — sign gains method, base64 and iso8601 timestamps: a scheme that signs the verb, encodes base64 and stamps an ISO instant had no way to be expressed — and every one of those mismatches fails as an authentication error that never names the format
- **v1.3.1** (2026-08-19) · `fix` — property names the MCP client refuses are aliased: it validates arguments against ^[a-zA-Z0-9_.-]{1,64}$ and rejects the whole CALL when one fails — PHP-style filters[offer_id] broke 7 of 9 tools on a real API
- **v1.3.0** (2026-08-19) · `feat` — per-request signature auth: APIs that sign every call over its own content — sha256 or hmac-sha256, with the payload and destination as templates
- **v1.2.0** (2026-08-19) · `feat` — secrets can come from the environment with env:NAME — a value passed as a flag is readable in ps and /proc/<pid>/cmdline by every process on the machine
- **v1.1.1** (2026-08-19) · `fix` — push.sh checks WRITE access, not just read: a public repo reads fine from any account, which is how the commits went up and the Release did not
- **v1.1.0** (2026-08-19) · `feat` — push.sh builds the release binaries and attaches them: five targets with checksums, built before anything is pushed
- **v1.0.1** (2026-08-19) · `chore` — root shortcuts for commit.sh and push.sh
- **v1.0.0** (2026-08-19) · `major` — serve any API as an MCP server from its specification: OpenAPI 3.x, Swagger 2.0 and GraphQL
